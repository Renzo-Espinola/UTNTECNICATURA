git log --oneline --all --graph (ver en modo grafico los commits doble guion)

git branch (ver ramas, marca con * la rama donde estamos posicionados)

git add . (agregar cambios) 

git commit -m "mensaje"  (comitear los cambios)

git switch (cambiar de rama)

git checkout -b "nombre rama" (crear una rama nueva)

git checkout nombreRama  (cambia de rama)

mkdir nombreCarpeta (crea una carpeta)

git status (detalla el estado de los archivos en la rama)

git init (inicia el repositorio) 

git merge --abort

git log (ver n° commit para volver)

git reset n° hash para volver
